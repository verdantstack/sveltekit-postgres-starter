[**sveltekit-postgres-starter**](../../../README.md)

***

# Function: newInviteToken()

> **newInviteToken**(): `string`

Generate a new cryptographically random invite token.

## Returns

`string`

A URL-safe base64 token. Only its SHA-256 digest is stored.
